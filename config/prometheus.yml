global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    monitor: 'rcig-monitor'

scrape_configs:
  - job_name: 'rcig'
    static_configs:
      - targets: ['rcig:8080']
    metrics_path: '/metrics'
    scrape_interval: 10s
